class SolverJson {
    constructor(points, constraints, movablePointsIds) {
        this.Points = points;
        this.Constraints = constraints;
        this.MovablePoints_id = movablePointsIds
    }
};

export default SolverJson;